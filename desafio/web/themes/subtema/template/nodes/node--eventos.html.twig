<h1> {{ node.label }} </h1>

{% if node.field_vagas_disponiveis.value > 0 %}

  <h4> {{ node.field_data_do_evento.value | date("d/m/Y") }} </h4>
  <pre> {{ node.field_descricao_do_evento.value }} </pre>
  <h5> {{ node.field_vagas_disponiveis.value }} Quantidade de vagas disponíveis </h5>

{% else %}

  <h1 style="color: #00009C;"> Desculpe, não há mais vagas para este evento! </h1>

{% endif %}